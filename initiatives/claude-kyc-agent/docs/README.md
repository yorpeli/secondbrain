# docs/ — Working Artifacts

All working materials for the Claude KYC Agent initiative go here. Yonatan drops
relevant materials in; Claude Code reads them directly.

Expected contents:
- `master-kyc-prompt-v2.2-runtime.md` — the current extraction prompt (and future versions)
- Japan validation results (the 91% vs Persona run) + future validation data
- The deck presented to Bea
- Research outputs (e.g. Google KYC capability research)

Note: only `CLAUDE.md` and `memory.md` sync to Supabase. Files in `docs/` do not
sync, but `context.md` + `docs/*.md` are embedded as `initiative_context` for
cross-initiative discovery.
